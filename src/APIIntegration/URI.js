const BASE_URL = "https://pokeapi.co/api/v2/";

const URI_MAP = {
  pokemons: {
    ability: `${BASE_URL}ability/`,
    characteristic: `${BASE_URL}characteristic/`,
    gender: `${BASE_URL}gender/`,
    pokemon: `${BASE_URL}pokemon/`,
  },
};

export default URI_MAP;
